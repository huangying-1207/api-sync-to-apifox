Index: README.md
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+># API 同步到 Apifox 工具 (v1.2.0)\r\n\r\n与 Apifox 项目同步后端接口，支持自动解析接口增删改查和手动触发同步，字段说明使用中文展示。支持增量同步和全量更新。\r\n\r\n## 更新历史\r\n\r\n### v1.2.0\r\n- 新增配置文件支持 (.apifoxsync.json)\r\n- 优化增量同步算法\r\n- 增强错误处理和用户体验\r\n- 新增 `config init` 命令初始化配置\r\n\r\n### v1.1.0\r\n- 新增代码解析功能\r\n- 支持增量同步\r\n- 添加接口变化比较功能\r\n\r\n### v1.0.0\r\n- 基础功能实现\r\n- 支持从代码和 Swagger 同步\r\n- 自动生成接口文档\r\n\r\n## 功能概述\r\n\r\n- **自动接口同步**：检测接口增删改查并同步到 Apifox\r\n- **中文字段说明**：自动格式化接口说明为中文\r\n- **增量同步**：只同步变更过的接口\r\n- **全量更新**：同步所有接口\r\n- **代码解析**：支持 Spring Boot 和 Node.js 项目\r\n\r\n## 快速开始\r\n\r\n### 安装依赖\r\n```bash\r\ncd api-sync-to-apifox && npm install\r\n```\r\n\r\n### 初始化配置\r\n```bash\r\napi-sync-to-apifox config init\r\n```\r\n\r\n### 扫描接口\r\n```bash\r\napi-sync-to-apifox scan\r\n```\r\n\r\n### 同步接口\r\n```bash\r\napi-sync-to-apifox sync\r\n```\r\n\r\n## 支持的命令\r\n\r\n### config\r\n管理配置文件\r\n```bash\r\napi-sync-to-apifox config init        # 初始化配置文件\r\n```\r\n\r\n### scan\r\n扫描接口变更\r\n```bash\r\napi-sync-to-apifox scan [参数]        # 扫描接口变更\r\n```\r\n\r\n**参数列表：**\r\n- --source-type <swagger|code> (必填): 接口源类型（swagger: Swagger文档，code: 代码解析）\r\n- --source-path <路径> (必填): 源路径（代码目录或 Swagger 文档 URL）\r\n- --framework <springboot|nodejs> (可选): 后端框架类型（当 source-type 为 code 时必填）\r\n- --scan-type <all|changed> (可选): 扫描类型（all: 所有接口，changed: 仅变更接口，默认: changed）\r\n\r\n**使用示例：**\r\n```bash\r\n# 从 Spring Boot 代码扫描所有接口\r\napi-sync-to-apifox scan --source-type code --source-path \"./src\" --framework springboot --scan-type all\r\n\r\n# 从 Node.js 代码扫描变更接口\r\napi-sync-to-apifox scan --source-type code --source-path \"./routes\" --framework nodejs --scan-type changed\r\n\r\n# 从 Swagger 文档扫描接口\r\napi-sync-to-apifox scan --source-type swagger --source-path \"https://api.example.com/v2/api-docs\"\r\n```\r\n\r\n### sync\r\n同步接口到 Apifox\r\n```bash\r\napi-sync-to-apifox sync [参数]        # 同步接口\r\napi-sync-to-apifox sync --sync-mode full  # 全量更新\r\n```\r\n\r\n**参数列表：**\r\n- --apifox-project-id <ID> (必填): Apifox 项目 ID\r\n- --apifox-api-key <API_KEY> (必填): Apifox API 密钥\r\n- --source-type <swagger|code> (必填): 接口源类型（swagger: Swagger文档，code: 代码解析）\r\n- --source-path <路径> (必填): 源路径（代码目录或 Swagger 文档 URL）\r\n- --framework <springboot|nodejs> (可选): 后端框架类型（当 source-type 为 code 时必填）\r\n- --sync-mode <incremental|full> (可选): 同步模式（incremental: 增量同步，full: 全量更新，默认: incremental）\r\n- --trigger-mode <auto|manual> (可选): 触发模式（auto: 自动触发，manual: 手动触发，默认: auto）\r\n\r\n**使用示例：**\r\n```bash\r\n# 使用配置文件同步接口\r\napi-sync-to-apifox sync\r\n\r\n# 从代码增量同步\r\napi-sync-to-apifox sync --apifox-project-id 12345 --apifox-api-key abc123 --source-type code --source-path \"./src\" --framework springboot --sync-mode incremental\r\n\r\n# 全量更新所有接口\r\napi-sync-to-apifox sync --apifox-project-id 12345 --apifox-api-key abc123 --source-type code --source-path \"./src\" --framework springboot --sync-mode full\r\n\r\n# 从 Swagger 同步\r\napi-sync-to-apifox sync --apifox-project-id 12345 --apifox-api-key abc123 --source-type swagger --source-path \"https://api.example.com/v2/api-docs\"\r\n```\r\n\r\n### help\r\n显示帮助信息\r\n```bash\r\napi-sync-to-apifox help               # 显示详细帮助\r\n```\r\n\r\n## 配置文件\r\n\r\n`.apifoxsync.json` 配置文件：\r\n```json\r\n{\r\n  \"apifox-project-id\": \"your-project-id\",\r\n  \"apifox-api-key\": \"your-api-key\",\r\n  \"source-type\": \"code\",\r\n  \"source-path\": \"./src\",\r\n  \"framework\": \"springboot\",\r\n  \"sync-mode\": \"incremental\"\r\n}\r\n```\r\n\r\n## 使用示例\r\n\r\n### 使用配置文件\r\n```bash\r\napi-sync-to-apifox config init       # 生成配置文件\r\napi-sync-to-apifox scan              # 扫描接口\r\napi-sync-to-apifox sync              # 同步接口\r\n```\r\n\r\n### 命令行参数\r\n```bash\r\n# 使用代码解析同步\r\napi-sync-to-apifox sync --apifox-project-id 12345 --apifox-api-key abc123 --source-type code --source-path \"./src\" --framework springboot --sync-mode incremental\r\n```\r\n\r\n## 注意事项\r\n\r\n- 代码解析基于 Git 变更检测，需要在 Git 仓库中使用\r\n- 增量同步只处理变更过的文件\r\n- 支持 Spring Boot 和 Node.js 项目\r\n\r\n## 相关文件\r\n\r\n- `help.txt`：详细使用说明\r\n- `config.js`：配置文件管理\r\n- `index.js`：工具主入口\r\n- `skill.json`：工具描述文件\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/README.md b/README.md
--- a/README.md	(revision fcef8c589c8ffc432e2fb5ccf200248854a49e71)
+++ b/README.md	(date 1778322659083)
@@ -1,9 +1,16 @@
-# API 同步到 Apifox 工具 (v1.2.0)
+# API 同步到 Apifox 工具 (v1.3.0)
 
 与 Apifox 项目同步后端接口，支持自动解析接口增删改查和手动触发同步，字段说明使用中文展示。支持增量同步和全量更新。
 
 ## 更新历史
 
+### v1.3.0
+- 新增 Django 框架支持
+- 增强错误处理机制，添加统一的错误处理接口
+- 优化项目代码结构，增加模块化设计
+- 新增配置验证功能，确保配置的完整性和正确性
+- 新增测试文件，覆盖核心功能测试
+
 ### v1.2.0
 - 新增配置文件支持 (.apifoxsync.json)
 - 优化增量同步算法
@@ -26,7 +33,7 @@
 - **中文字段说明**：自动格式化接口说明为中文
 - **增量同步**：只同步变更过的接口
 - **全量更新**：同步所有接口
-- **代码解析**：支持 Spring Boot 和 Node.js 项目
+- **代码解析**：支持 Spring Boot、Node.js 和 Django 项目
 
 ## 快速开始
 
@@ -67,7 +74,7 @@
 **参数列表：**
 - --source-type <swagger|code> (必填): 接口源类型（swagger: Swagger文档，code: 代码解析）
 - --source-path <路径> (必填): 源路径（代码目录或 Swagger 文档 URL）
-- --framework <springboot|nodejs> (可选): 后端框架类型（当 source-type 为 code 时必填）
+- --framework <springboot|nodejs|django> (可选): 后端框架类型（当 source-type 为 code 时必填）
 - --scan-type <all|changed> (可选): 扫描类型（all: 所有接口，changed: 仅变更接口，默认: changed）
 
 **使用示例：**
@@ -78,6 +85,9 @@
 # 从 Node.js 代码扫描变更接口
 api-sync-to-apifox scan --source-type code --source-path "./routes" --framework nodejs --scan-type changed
 
+# 从 Django 代码扫描所有接口
+api-sync-to-apifox scan --source-type code --source-path "./django_project" --framework django --scan-type all
+
 # 从 Swagger 文档扫描接口
 api-sync-to-apifox scan --source-type swagger --source-path "https://api.example.com/v2/api-docs"
 ```
@@ -94,7 +104,7 @@
 - --apifox-api-key <API_KEY> (必填): Apifox API 密钥
 - --source-type <swagger|code> (必填): 接口源类型（swagger: Swagger文档，code: 代码解析）
 - --source-path <路径> (必填): 源路径（代码目录或 Swagger 文档 URL）
-- --framework <springboot|nodejs> (可选): 后端框架类型（当 source-type 为 code 时必填）
+- --framework <springboot|nodejs|django> (可选): 后端框架类型（当 source-type 为 code 时必填）
 - --sync-mode <incremental|full> (可选): 同步模式（incremental: 增量同步，full: 全量更新，默认: incremental）
 - --trigger-mode <auto|manual> (可选): 触发模式（auto: 自动触发，manual: 手动触发，默认: auto）
 
@@ -111,6 +121,9 @@
 
 # 从 Swagger 同步
 api-sync-to-apifox sync --apifox-project-id 12345 --apifox-api-key abc123 --source-type swagger --source-path "https://api.example.com/v2/api-docs"
+
+# 从 Django 代码同步接口
+api-sync-to-apifox sync --apifox-project-id 12345 --apifox-api-key abc123 --source-type code --source-path "./django_project" --framework django --sync-mode incremental
 ```
 
 ### help
@@ -129,7 +142,9 @@
   "source-type": "code",
   "source-path": "./src",
   "framework": "springboot",
-  "sync-mode": "incremental"
+  "sync-mode": "incremental",
+  "trigger-mode": "auto",
+  "scan-type": "changed"
 }
 ```
 
@@ -148,11 +163,166 @@
 api-sync-to-apifox sync --apifox-project-id 12345 --apifox-api-key abc123 --source-type code --source-path "./src" --framework springboot --sync-mode incremental
 ```
 
+## 支持的框架
+
+### Spring Boot
+- 识别 `@RestController` 注解的类
+- 识别 `@GetMapping`、`@PostMapping`、`@PutMapping`、`@DeleteMapping` 注解的方法
+
+### Node.js
+- 识别 `app.get()`、`app.post()`、`app.put()`、`app.delete()` 等路由方法
+- 支持 Express 框架
+
+### Django
+- 识别 `urls.py` 中的 path() 函数定义的路由
+- 支持 GET、POST、PUT、DELETE 等 HTTP 方法
+
+## 配置选项说明
+
+| 配置项 | 必填 | 说明 | 可选值 | 默认值 |
+|--------|------|------|--------|--------|
+| apifox-project-id | ✔️ | Apifox 项目 ID | - | - |
+| apifox-api-key | ✔️ | Apifox API 密钥 | - | - |
+| source-type | ✔️ | 数据源类型 | swagger\|code | code |
+| source-path | ✔️ | 源路径 | 文件/目录路径或 URL | ./src |
+| framework | ✔️（当 source-type 为 code 时） | 后端框架类型 | springboot\|nodejs\|django | springboot |
+| sync-mode | ❌ | 同步模式 | incremental\|full | incremental |
+| trigger-mode | ❌ | 触发模式 | auto\|manual | auto |
+| scan-type | ❌ | 扫描类型 | all\|changed | changed |
+
+## 故障排除
+
+### 1. 权限问题
+如果遇到权限问题，尝试以下方法：
+```bash
+# 修复权限
+chmod +x node_modules/api-sync-to-apifox/src/index.js
+
+# 重新安装
+npm uninstall api-sync-to-apifox
+npm install file:path/to/api-sync-to-apifox-1.3.0.tgz --save-dev
+```
+
+### 2. Node.js 版本兼容性
+确保您使用的 Node.js 版本符合要求：
+```bash
+node --version
+# 应该是 12.0 或更高版本
+```
+
+### 3. Git 环境问题
+增量同步功能需要在 Git 仓库中使用：
+```bash
+git status
+# 如果不是 git 仓库，需要初始化
+git init && git add . && git commit -m "Initial commit"
+```
+
+### 4. 配置文件问题
+如果配置文件找不到或格式不正确：
+```bash
+# 初始化配置文件
+api-sync-to-apifox config init
+
+# 检查配置文件格式
+cat .apifoxsync.json
+```
+
+## 常见问题
+
+### 问：配置文件找不到怎么办？
+答：工具会在以下位置查找配置文件：
+1. 当前目录下的 `.apifoxsync.json`
+2. `./.claude/apifoxsync.json`
+3. `./config/apifoxsync.json`
+4. 用户主目录下的 `.apifoxsync.json`
+
+### 问：同步过程中出现网络错误怎么办？
+答：确保您的网络可以正常访问 Apifox API。如果使用代理，请配置代理：
+```bash
+export https_proxy=http://proxy.example.com:8080
+```
+
+### 问：接口没有被正确识别怎么办？
+答：
+1. 确保代码中使用了标准的注解（Spring Boot）或路由方法（Node.js/Django）
+2. 检查 `source-path` 是否正确
+3. 尝试使用 `--scan-type all` 参数扫描所有文件
+
+## CI/CD 集成
+
+在 CI/CD 流程中使用（以 GitHub Actions 为例）：
+```yaml
+name: Apifox Sync
+
+on:
+  push:
+    branches: [main]
+
+jobs:
+  sync:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v3
+      
+      - name: Set up Node.js
+        uses: actions/setup-node@v3
+        with:
+          node-version: '16'
+      
+      - name: Install dependencies
+        run: npm ci
+      
+      - name: Sync to Apifox
+        run: |
+          cat > .apifoxsync.json << 'EOF'
+          {
+            "apifox-project-id": "${{ secrets.APIFOX_PROJECT_ID }}",
+            "apifox-api-key": "${{ secrets.APIFOX_API_KEY }}",
+            "source-type": "code",
+            "source-path": "./src/main/java",
+            "framework": "springboot"
+          }
+          EOF
+          
+          npx api-sync-to-apifox sync
+```
+
+## 项目架构
+
+### 核心模块
+- `src/index.js`：工具主入口，处理命令行参数和命令分发
+- `src/modules/scanner.js`：代码扫描模块，支持 Spring Boot、Node.js 和 Django
+- `src/modules/parser.js`：接口文档解析模块
+- `src/modules/formatter.js`：接口文档格式化模块
+- `src/modules/comparer.js`：接口变化比较模块
+- `src/modules/syncer.js`：与 Apifox API 通信模块
+- `src/utils/errorHandler.js`：统一错误处理模块
+- `src/utils/configValidator.js`：配置验证模块
+
+### 依赖
+- `axios`：HTTP 请求库
+- `glob`：文件匹配库
+- `js-yaml`：YAML 文件解析库
+- `yaml`：YAML 文件解析库
+
+## 测试
+
+项目包含以下测试文件：
+- `test/helper.test.js`：测试辅助函数
+- `test/errorHandler.test.js`：测试错误处理机制
+- `test/scanner.test.js`：测试代码扫描功能
+
+### 运行测试
+```bash
+npm run test
+```
+
 ## 注意事项
 
-- 代码解析基于 Git 变更检测，需要在 Git 仓库中使用
+- 代码解析功能基于 Git 变更检测，需要在 Git 仓库中使用
 - 增量同步只处理变更过的文件
-- 支持 Spring Boot 和 Node.js 项目
+- 支持 Spring Boot、Node.js 和 Django 项目
 
 ## 相关文件
 
